---
title: "A beutiful Graphic"
format: html
author: Nicolas
execute: echo: false
---


Nicolas is a data maestro!

```{r}
#| message: false
library(tidyverse)
```

```{r}
ggplot(mtcars, aes(x = hp, y = mpg, color = factor(cyl))) +
  geom_point(size = 3, alpha = 0.7) +
  geom_smooth(method = "lm", se = FALSE, alpha = 0.3) +
  scale_color_brewer(palette = "Set1", name = "Cylinders") +
  labs(
    title = "Fuel Efficiency vs Horsepower",
    subtitle = "by Number of Cylinders",
    x = "Horsepower",
    y = "Miles per Gallon",
    caption = "Source: mtcars dataset"
  ) +
  theme_minimal() +
  theme(
    plot.title = element_text(size = 14, face = "bold"),
    plot.subtitle = element_text(size = 11),
    legend.position = "top"
  )
```
